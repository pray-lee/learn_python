# 函数进阶

# 命名空间和作用域

#内置命名空间
    # python解释器启动的时候就被加载到内存里
# 全局命名空间
    # 程序从上到下执行的过程中依次加载到内存的
    # 放置我们设置的所有变量名和函数名
# 局部命名空间
    # 函数内部
    # 函数调用的时候，才会产生局部命名空间

globals() # 查看全局的变量, 只是全局的（全局加内置）
locals() # 查看局部的变量, 根据当前所在的环境
